(ocamllex
 (modules lexer))

(menhir
 (modules start_parser parser)
 (flags --table --inspection --explain --dump))

(library
  (name greta)
  (modes byte native)
  (libraries unix num menhirLib str))
(ocamllex
 (modules lexer))

(menhir
 (flags --table --inspection --dump --explain --reference-graph --greta) 	
 (modules parser))

(library
  (name g3_old)
  (modes byte native)
  (libraries menhirLib str core graphics qcheck num))

** Conflict (reduce/reduce) in state 12.
** Tokens involved: PLUS MUL ELSE
** The following explanations concentrate on token MUL.
** This state is reached from prog1 after reading:

IF cond5 THEN IF cond5 THEN expr4

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 12, looking ahead at MUL, reducing production..
** expr3 -> IF cond5 THEN expr4
** is permitted because of the following sub-derivation:

IF cond5 THEN expr1 ELSE expr1 
              expr3 MUL expr4 // lookahead token appears
              IF cond5 THEN expr4 . 

** In state 12, looking ahead at MUL, reducing production..
** expr4 -> IF cond5 THEN expr4
** is permitted because of the following sub-derivation:

expr3 MUL expr4 // lookahead token appears
IF cond5 THEN expr4 // lookahead token is inherited
              IF cond5 THEN expr4 . 

** Conflict (reduce/reduce) in state 11.
** Tokens involved: PLUS MUL ELSE
** The following explanations concentrate on token MUL.
** This state is reached from prog1 after reading:

IF cond5 THEN expr6

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 11, looking ahead at MUL, reducing production..
** expr3 -> expr6
** is permitted because of the following sub-derivation:

IF cond5 THEN expr1 ELSE expr1 
              expr3 MUL expr4 // lookahead token appears
              expr6 . 

** In state 11, looking ahead at MUL, reducing production..
** expr4 -> expr6
** is permitted because of the following sub-derivation:

expr3 MUL expr4 // lookahead token appears
IF cond5 THEN expr4 // lookahead token is inherited
              expr6 . 

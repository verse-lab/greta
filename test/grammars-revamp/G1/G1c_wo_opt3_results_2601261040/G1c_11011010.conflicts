
** Conflict (shift/reduce) in state 60.
** Token involved: PLUS
** This state is reached from prog1 after reading:

IF cond6 THEN expr3

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 60, looking ahead at PLUS, shifting is permitted..
** because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              expr3 . PLUS expr2 

** In state 60, looking ahead at PLUS, reducing production..
** expr3 -> IF cond6 THEN expr3
** is permitted because of the following sub-derivation:

expr3 PLUS expr2 // lookahead token appears
IF cond6 THEN expr3 . 

** Conflict (shift/reduce) in state 56.
** Tokens involved: PLUS ELSE
** The following explanations concentrate on token PLUS.
** This state is reached from prog1 after reading:

IF cond6 THEN IF cond6 THEN expr3

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
IF cond6 THEN expr1 ELSE expr1 
              (?)

** In state 56, looking ahead at PLUS, shifting is permitted..
** because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              expr3 . PLUS expr2 

** In state 56, looking ahead at PLUS, reducing production..
** expr3 -> IF cond6 THEN expr3
** is permitted because of the following sub-derivation:

expr3 PLUS expr2 // lookahead token appears
IF cond6 THEN expr3 . 

** Conflict (reduce/reduce) in state 55.
** Token involved: ELSE
** This state is reached from prog1 after reading:

IF cond6 THEN IF cond6 THEN expr5 MUL expr4

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 55, looking ahead at ELSE, reducing production..
** expr1 -> expr5 MUL expr4
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              IF cond6 THEN expr1 ELSE expr1 // lookahead token appears
                            expr5 MUL expr4 . 

** In state 55, looking ahead at ELSE, reducing production..
** expr3 -> expr5 MUL expr4
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 // lookahead token appears
              IF cond6 THEN expr3 // lookahead token is inherited
                            expr5 MUL expr4 . 

** Conflict (reduce/reduce) in state 49.
** Token involved: ELSE
** This state is reached from prog1 after reading:

IF cond6 THEN IF cond6 THEN INT

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 49, looking ahead at ELSE, reducing production..
** expr1 -> INT
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              IF cond6 THEN expr1 ELSE expr1 // lookahead token appears
                            INT . 

** In state 49, looking ahead at ELSE, reducing production..
** expr3 -> INT
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 // lookahead token appears
              IF cond6 THEN expr3 // lookahead token is inherited
                            INT . 

** Conflict (reduce/reduce) in state 48.
** Token involved: ELSE
** This state is reached from prog1 after reading:

IF cond6 THEN IF cond6 THEN LPAREN expr1 RPAREN

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 48, looking ahead at ELSE, reducing production..
** expr1 -> LPAREN expr1 RPAREN
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              IF cond6 THEN expr1 ELSE expr1 // lookahead token appears
                            LPAREN expr1 RPAREN . 

** In state 48, looking ahead at ELSE, reducing production..
** expr3 -> LPAREN expr1 RPAREN
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 // lookahead token appears
              IF cond6 THEN expr3 // lookahead token is inherited
                            LPAREN expr1 RPAREN . 

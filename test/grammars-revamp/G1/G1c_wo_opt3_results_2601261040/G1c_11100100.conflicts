
** Conflict (reduce/reduce) in state 39.
** Tokens involved: MUL ELSE
** The following explanations concentrate on token MUL.
** This state is reached from prog1 after reading:

IF cond6 THEN IF cond6 THEN expr3

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 39, looking ahead at MUL, reducing production..
** expr2 -> IF cond6 THEN expr3
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              expr2 MUL expr3 // lookahead token appears
              IF cond6 THEN expr3 . 

** In state 39, looking ahead at MUL, reducing production..
** expr3 -> IF cond6 THEN expr3
** is permitted because of the following sub-derivation:

expr2 MUL expr3 // lookahead token appears
IF cond6 THEN expr3 // lookahead token is inherited
              IF cond6 THEN expr3 . 

** Conflict (reduce/reduce) in state 38.
** Tokens involved: MUL ELSE
** The following explanations concentrate on token MUL.
** This state is reached from prog1 after reading:

IF cond6 THEN expr4 PLUS expr5

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 38, looking ahead at MUL, reducing production..
** expr2 -> expr4 PLUS expr5
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              expr2 MUL expr3 // lookahead token appears
              expr4 PLUS expr5 . 

** In state 38, looking ahead at MUL, reducing production..
** expr3 -> expr4 PLUS expr5
** is permitted because of the following sub-derivation:

expr2 MUL expr3 // lookahead token appears
IF cond6 THEN expr3 // lookahead token is inherited
              expr4 PLUS expr5 . 

** Conflict (reduce/reduce) in state 32.
** Tokens involved: MUL ELSE
** The following explanations concentrate on token MUL.
** This state is reached from prog1 after reading:

IF cond6 THEN INT

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 32, looking ahead at MUL, reducing production..
** expr2 -> INT
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              expr2 MUL expr3 // lookahead token appears
              INT . 

** In state 32, looking ahead at MUL, reducing production..
** expr3 -> INT
** is permitted because of the following sub-derivation:

expr2 MUL expr3 // lookahead token appears
IF cond6 THEN expr3 // lookahead token is inherited
              INT . 

** Conflict (reduce/reduce) in state 31.
** Tokens involved: MUL ELSE
** The following explanations concentrate on token MUL.
** This state is reached from prog1 after reading:

IF cond6 THEN LPAREN expr1 RPAREN

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr1 EOF 
(?)

** In state 31, looking ahead at MUL, reducing production..
** expr2 -> LPAREN expr1 RPAREN
** is permitted because of the following sub-derivation:

IF cond6 THEN expr1 ELSE expr1 
              expr2 MUL expr3 // lookahead token appears
              LPAREN expr1 RPAREN . 

** In state 31, looking ahead at MUL, reducing production..
** expr3 -> LPAREN expr1 RPAREN
** is permitted because of the following sub-derivation:

expr2 MUL expr3 // lookahead token appears
IF cond6 THEN expr3 // lookahead token is inherited
              LPAREN expr1 RPAREN . 

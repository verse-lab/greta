
** Conflict (reduce/reduce) in state 31.
** Token involved: ELSE
** This state is reached from prog1 after reading:

IF cond4 THEN IF cond4 THEN IF cond4 THEN expr2

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr6 EOF 
expr1 
(?)

** In state 31, looking ahead at ELSE, reducing production..
** expr1 -> IF cond4 THEN expr2
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 
              IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
                            IF cond4 THEN expr2 . 

** In state 31, looking ahead at ELSE, reducing production..
** expr2 -> IF cond4 THEN expr2
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
              IF cond4 THEN expr2 // lookahead token is inherited
                            IF cond4 THEN expr2 . 

** Conflict (reduce/reduce) in state 30.
** Token involved: ELSE
** This state is reached from prog1 after reading:

IF cond4 THEN IF cond4 THEN expr5 MUL expr3

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr6 EOF 
expr1 
(?)

** In state 30, looking ahead at ELSE, reducing production..
** expr1 -> expr5 MUL expr3
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 
              IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
                            expr5 MUL expr3 . 

** In state 30, looking ahead at ELSE, reducing production..
** expr2 -> expr5 MUL expr3
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
              IF cond4 THEN expr2 // lookahead token is inherited
                            expr5 MUL expr3 . 

** Conflict (reduce/reduce) in state 24.
** Token involved: ELSE
** This state is reached from prog1 after reading:

IF cond4 THEN IF cond4 THEN INT

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr6 EOF 
expr1 
(?)

** In state 24, looking ahead at ELSE, reducing production..
** expr1 -> INT
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 
              IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
                            INT . 

** In state 24, looking ahead at ELSE, reducing production..
** expr2 -> INT
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
              IF cond4 THEN expr2 // lookahead token is inherited
                            INT . 

** Conflict (reduce/reduce) in state 10.
** Token involved: ELSE
** This state is reached from prog1 after reading:

IF cond4 THEN IF cond4 THEN LPAREN expr6 RPAREN

** The derivations that appear below have the following common factor:
** (The question mark symbol (?) represents the spot where the derivations begin to differ.)

prog1 
expr6 EOF 
expr1 
(?)

** In state 10, looking ahead at ELSE, reducing production..
** expr1 -> LPAREN expr6 RPAREN
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 
              IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
                            LPAREN expr6 RPAREN . 

** In state 10, looking ahead at ELSE, reducing production..
** expr2 -> LPAREN expr6 RPAREN
** is permitted because of the following sub-derivation:

IF cond4 THEN expr1 ELSE expr1 // lookahead token appears
              IF cond4 THEN expr2 // lookahead token is inherited
                            LPAREN expr6 RPAREN . 
